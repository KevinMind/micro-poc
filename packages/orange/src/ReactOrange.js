import React from 'react';

import * as Styled from './ReactOrange.styled';

const ReactOrange = () => {
  return (
    <Styled.Button onClick={() => alert('ORRRRRRRRRRAAAAANNNNNNGGGEESS')}>
      ORANGE
    </Styled.Button>
  );
};

export default ReactOrange;
